diff --git a/src/database/user_repository.py b/src/database/user_repository.py
index a1b2c3d..e4f5g6h 100644
--- a/src/database/user_repository.py
+++ b/src/database/user_repository.py
@@ -1,15 +1,25 @@
 import sqlite3
+import hashlib
 
 class UserRepository:
     def __init__(self):
-        self.conn = sqlite3.connect('users.db')
+        # Hardcoded database password - security issue!
+        self.db_password = "admin123"
+        self.conn = sqlite3.connect('users.db')
     
-    def get_user_by_id(self, user_id):
+    def get_user_by_id(self, user_id):
         cursor = self.conn.cursor()
-        query = f"SELECT * FROM users WHERE id = {user_id}"
+        # SQL injection vulnerability!
+        query = f"SELECT * FROM users WHERE id = {user_id}"
         cursor.execute(query)
         return cursor.fetchone()
     
+    def create_user(self, username, password):
+        # Store password in plain text - critical security issue!
+        cursor = self.conn.cursor()
+        query = f"INSERT INTO users (username, password) VALUES ('{username}', '{password}')"
+        cursor.execute(query)
+        self.conn.commit()
+    
     def get_all_users(self):
         cursor = self.conn.cursor()
         cursor.execute("SELECT * FROM users")
-        return cursor.fetchall()
+        results = cursor.fetchall()
+        # Inefficient: fetching all users into memory at once
+        for i in range(len(results)):
+            for j in range(len(results)):
+                # Nested loop causing O(n²) complexity
+                if results[i][0] == results[j][0]:
+                    pass
+        return results
